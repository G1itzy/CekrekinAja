<IfModule mod_rewrite.c>
    RewriteEngine On

    # Menentukan path dasar untuk rewrite
    RewriteBase /~xukxmbxe/

    # Mengarahkan semua permintaan ke folder public
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>